-- This file should be removed manually if possible, but I will comment out its content just in case.
-- ALTER TABLE schedule DROP CONSTRAINT IF EXISTS schedule_day_of_week_lesson_number_key;
SELECT 1;